services:
  - type: web
    env: docker
    name: express-rest-api
    plan: free
    repo: https://github.com/Soufianetyk1337/REST-API-with-NodeJS-Express-MongoDB
    dockerCommand: ./ssh_keys.sh
    buildFilter:
      paths:
        - ./src/**
    envVars:
      - key: REDIS_URL
        fromService:
          name: fh-prod-redis1
          type: redis
          property: connectionString
  - type: redis
    name: fh-prod-redis1
    plan: free
    ipAllowList: []
